# Green Bond Platform

[![Java](https://img.shields.io/badge/Java-17-orange.svg)](https://www.oracle.com/java/)
[![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.2-brightgreen.svg)](https://spring.io/projects/spring-boot)
[![Docker](https://img.shields.io/badge/Docker-Ready-blue.svg)](https://www.docker.com/)
[![Swagger](https://img.shields.io/badge/Swagger-OpenAPI-green.svg)](http://localhost:8080/swagger-ui.html)
[![GitHub](https://img.shields.io/badge/GitHub-Repository-black.svg)](https://github.com/speculum-factorem/greenBondPlatform.git)

## Описание

**Green Bond Platform** — это комплексная микросервисная платформа для цифрового управления зелеными облигациями (Green Bonds) с интеграцией блокчейна и IoT-устройств. Платформа обеспечивает полный жизненный цикл зеленых облигаций: от выпуска до мониторинга ESG-метрик и верификации соответствия стандартам.

### Основная цель

Платформа решает проблему **прозрачности и доверия** на рынке зеленых облигаций, предоставляя:

- **Неизменяемость данных** через блокчейн
- **Реальный мониторинг** ESG-метрик через IoT
- **Автоматизированную верификацию** документов
- **Прозрачность** для всех участников рынка

## Архитектура

### Микросервисная архитектура (7 модулей):

1. **API Gateway** (порт 8080)

   - Единая точка входа для всех клиентов
   - JWT аутентификация и авторизация
   - Маршрутизация запросов к микросервисам
   - Централизованная обработка ошибок
   - Swagger документация

2. **Bond Issuance Service** (порт 8081)

   - Создание и управление облигациями
   - Токенизация облигаций на блокчейне
   - Распределение облигаций между инвесторами
   - PostgreSQL + Flyway для миграций

3. **Impact Monitoring Service** (порт 8084)

   - Мониторинг 33+ типов ESG-метрик
   - Интеграция с IoT-устройствами
   - Оценка качества данных
   - Временные ряды (InfluxDB) + MongoDB

4. **Document Verification Service** (порт 8083)

   - Загрузка и обработка документов (Apache Tika)
   - Автоматическое извлечение полей
   - Верификация аудиторами
   - MongoDB для хранения

5. **Blockchain Integration** (порт 8082, gRPC 9090)

   - gRPC сервис для токенизации
   - Web3j интеграция с Ethereum
   - Управление смарт-контрактами
   - Запись метрик в блокчейн

6. **Platform Core**

   - Общие утилиты и модели
   - MapStruct для маппинга
   - Валидация и JSON обработка
   - MDC утилиты для логирования

7. **Frontend** (в разработке)
   - Веб-интерфейс для всех ролей

### Архитектурная диаграмма

```
┌─────────────────────────────────────────────────────────────────┐
│                         Клиенты (Web/Mobile)                    │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                      API Gateway (8080)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │   JWT Auth   │  │   Routing    │  │   Error Handling     │   │
│  └──────────────┘  └──────────────┘  └──────────────────────┘   │
└───────┬──────────────┬──────────────┬──────────────┬────────────┘
        │              │              │              │
        ▼              ▼              ▼              ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│   Bond       │ │  Document    │ │   Impact     │ │ Blockchain   │
│  Issuance    │ │ Verification │ │ Monitoring   │ │ Integration  │
│  (8081)      │ │   (8083)     │ │   (8084)     │ │  (8082/9090) │
└──────┬───────┘ └──────┬───────┘ └──────┬───────┘ └──────┬───────┘
       │                │                │                │
       ▼                ▼                ▼                ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ PostgreSQL   │ │   MongoDB    │ │   MongoDB    │ │   Ethereum   │
│              │ │              │ │  + InfluxDB  │ │  Blockchain  │
└──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘
```

### Поток данных

1. **Аутентификация**: Клиент → API Gateway → JWT токен
2. **Создание облигации**: API Gateway → Bond Issuance → PostgreSQL → Blockchain Integration
3. **Загрузка документа**: API Gateway → Document Verification → MongoDB → Асинхронная обработка
4. **Мониторинг метрик**: API Gateway → Impact Monitoring → MongoDB + InfluxDB → Blockchain Integration
5. **Верификация**: Document Verification → Blockchain Integration (запись хеша)

### Межсервисная коммуникация

- **HTTP/REST**: Основной протокол для синхронной коммуникации
- **gRPC**: Для высокопроизводительной коммуникации с Blockchain Integration
- **Асинхронная обработка**: Spring @Async для фоновых задач
- **Event-driven**: Готовность к интеграции с message broker (RabbitMQ/Kafka)

## Сущности и их жизненный цикл

### 1. Bond (Облигация)

**Описание**: Основная сущность платформы, представляющая зеленую облигацию.

**Основные атрибуты**:
- `bondId` - уникальный идентификатор облигации
- `projectName` - название проекта
- `totalSupply` - общий объем выпуска
- `faceValue` - номинальная стоимость
- `couponRate` - процентная ставка
- `maturityDate` - дата погашения
- `issuerId` - ID эмитента
- `blockchainTxHash` - хеш транзакции в блокчейне
- `bondContractAddress` - адрес смарт-контракта

**Жизненный цикл (BondStatus)**:

```
DRAFT → PENDING_VERIFICATION → VERIFIED → TOKENIZED → ACTIVE
                                                              ↓
                                                         MATURED
                                                              ↓
                                                         DEFAULTED (если дефолт)
                                                         
Или: DRAFT → CANCELLED (отмена)
     ACTIVE → SUSPENDED (приостановка)
```

**Переходы состояний**:

1. **DRAFT** → **PENDING_VERIFICATION**
   - Триггер: Эмитент отправляет облигацию на верификацию
   - Условие: Все обязательные документы загружены

2. **PENDING_VERIFICATION** → **VERIFIED**
   - Триггер: Аудитор подтверждает соответствие стандартам
   - Условие: Все документы верифицированы, метрики соответствуют целям

3. **VERIFIED** → **TOKENIZED**
   - Триггер: Инициируется токенизация на блокчейне
   - Условие: Успешное создание смарт-контракта

4. **TOKENIZED** → **ACTIVE**
   - Триггер: Облигация доступна для инвестирования
   - Условие: Токенизация завершена, контракт активен

5. **ACTIVE** → **MATURED**
   - Триггер: Достигнута дата погашения
   - Автоматический переход

6. **ACTIVE** → **SUSPENDED**
   - Триггер: Нарушение условий или административное решение
   - Может быть восстановлена в ACTIVE

7. **ACTIVE** → **DEFAULTED**
   - Триггер: Невыполнение обязательств эмитентом

**Взаимодействие**:
- Связана с **Document** (множество документов для верификации)
- Связана с **ImpactMetric** (метрики воздействия проекта)
- Связана с **ImpactGoal** (цели по ESG-метрикам)
- Интегрируется с **Blockchain** (токенизация и неизменяемость)

### 2. Document (Документ)

**Описание**: Документы, необходимые для верификации облигации (ESG отчеты, финансовые отчеты, проектные предложения).

**Основные атрибуты**:
- `documentId` - уникальный идентификатор документа
- `bondId` - связанная облигация
- `documentType` - тип документа (ESG_REPORT, FINANCIAL_STATEMENT, PROJECT_PROPOSAL)
- `filePath` - путь к файлу
- `fileHash` - SHA-256 хеш файла для проверки целостности
- `extractedFields` - автоматически извлеченные поля
- `verificationSteps` - шаги верификации
- `verifierId` - ID верификатора

**Жизненный цикл (DocumentStatus)**:

```
UPLOADED → PROCESSING → EXTRACTION_COMPLETED → VERIFICATION_PENDING
                                                      ↓
                                              UNDER_REVIEW (опционально)
                                                      ↓
                                              VERIFIED / REJECTED
                                                      ↓
                                              EXPIRED / ARCHIVED
```

**Переходы состояний**:

1. **UPLOADED** → **PROCESSING**
   - Триггер: Автоматически после загрузки
   - Действие: Асинхронная обработка Apache Tika

2. **PROCESSING** → **EXTRACTION_COMPLETED**
   - Триггер: Успешное извлечение метаданных и полей
   - Действие: Сохранение извлеченных данных

3. **EXTRACTION_COMPLETED** → **VERIFICATION_PENDING**
   - Триггер: Документ готов к верификации
   - Условие: Извлечены все необходимые поля

4. **VERIFICATION_PENDING** → **UNDER_REVIEW**
   - Триггер: Запрошен дополнительный обзор
   - Инициатор: Аудитор или эмитент

5. **VERIFICATION_PENDING/UNDER_REVIEW** → **VERIFIED**
   - Триггер: Аудитор подтверждает документ
   - Условие: Все поля проверены и соответствуют стандартам

6. **VERIFICATION_PENDING/UNDER_REVIEW** → **REJECTED**
   - Триггер: Аудитор отклоняет документ
   - Причина: Несоответствие стандартам или неполные данные

7. **VERIFIED/REJECTED** → **EXPIRED**
   - Триггер: Истечение срока действия документа
   - Автоматический переход по истечении срока

8. **VERIFIED/REJECTED/EXPIRED** → **ARCHIVED**
   - Триггер: Документ архивируется после завершения жизненного цикла облигации

**Взаимодействие**:
- Принадлежит **Bond** (множество документов на одну облигацию)
- Верифицируется **User** с ролью AUDITOR
- Загружается **User** с ролью ISSUER
- Хеш записывается в **Blockchain** для неизменяемости

### 3. ImpactMetric (Метрика воздействия)

**Описание**: ESG-метрики, измеряющие воздействие проекта на окружающую среду, общество и экономику.

**Основные атрибуты**:
- `metricId` - уникальный идентификатор метрики
- `bondId` - связанная облигация
- `metricType` - тип метрики (33+ типов: CARBON_EMISSIONS_REDUCTION, RENEWABLE_ENERGY_PRODUCTION и др.)
- `value` - значение метрики
- `unit` - единица измерения
- `timestamp` - время измерения
- `dataQuality` - оценка качества данных
- `sourceType` - источник данных (IoT, MANUAL, EXTERNAL)
- `blockchainTxHash` - хеш транзакции в блокчейне

**Типы метрик**:

- **Экологические**: сокращение выбросов CO2, производство возобновляемой энергии, экономия воды, сокращение отходов
- **Социальные**: созданные рабочие места, вовлеченность сообщества, улучшение здоровья, доступ к образованию
- **Экономические**: рост местной экономики, налоговые поступления, экономия затрат

**Жизненный цикл**:

```
Создание → Валидация → Оценка качества → Сохранение в MongoDB → 
Сохранение в InfluxDB (временные ряды) → Запись в блокчейн
```

**Процесс создания**:

1. **Создание метрики**
   - Источник: IoT устройство, ручной ввод, внешняя система
   - Валидация: Проверка формата, диапазона значений

2. **Оценка качества данных**
   - Автоматическая оценка: полнота, точность, актуальность
   - Результат: `DataQuality` объект с оценками

3. **Сохранение**
   - MongoDB: Для запросов и аналитики
   - InfluxDB: Для временных рядов и графиков
   - Blockchain: Для неизменяемости и прозрачности

**Взаимодействие**:
- Принадлежит **Bond** (множество метрик на одну облигацию)
- Связана с **ImpactGoal** (сравнение с целевыми значениями)
- Интегрируется с **IoT** устройствами для автоматического сбора
- Записывается в **Blockchain** для прозрачности

### 4. ImpactGoal (Цель воздействия)

**Описание**: Целевые значения ESG-метрик, которые должны быть достигнуты проектом.

**Основные атрибуты**:
- `goalId` - уникальный идентификатор цели
- `bondId` - связанная облигация
- `metricType` - тип метрики
- `targetValue` - целевое значение
- `currentValue` - текущее значение
- `progressPercentage` - процент выполнения
- `targetDate` - целевая дата достижения
- `status` - статус цели (DRAFT, ACTIVE, ACHIEVED, FAILED, EXPIRED)

**Жизненный цикл (GoalStatus)**:

```
DRAFT → ACTIVE → ACHIEVED / FAILED / EXPIRED
```

**Переходы состояний**:

1. **DRAFT** → **ACTIVE**
   - Триггер: Цель активирована при запуске проекта
   - Условие: Облигация в статусе ACTIVE

2. **ACTIVE** → **ACHIEVED**
   - Триггер: `currentValue >= targetValue` до `targetDate`
   - Автоматическая проверка при обновлении метрик

3. **ACTIVE** → **FAILED**
   - Триггер: `targetDate` прошла, но цель не достигнута
   - Автоматический переход

4. **ACTIVE** → **EXPIRED**
   - Триггер: Цель отменена или срок истек

**Взаимодействие**:
- Принадлежит **Bond**
- Сравнивается с **ImpactMetric** для расчета прогресса
- Используется в **ImpactReport** для отчетности

### 5. ImpactReport (Отчет о воздействии)

**Описание**: Периодические отчеты о прогрессе проекта по ESG-метрикам.

**Основные атрибуты**:
- `reportId` - уникальный идентификатор отчета
- `bondId` - связанная облигация
- `reportType` - тип отчета (QUARTERLY, ANNUAL, AD_HOC)
- `reportingPeriodStart/End` - период отчета
- `metricSummaries` - сводка по метрикам
- `goalProgress` - прогресс по целям
- `blockchainTxHash` - хеш в блокчейне

**Жизненный цикл (ReportStatus)**:

```
DRAFT → GENERATING → GENERATED → UNDER_REVIEW → APPROVED → PUBLISHED → ARCHIVED
```

**Взаимодействие**:
- Агрегирует данные из **ImpactMetric**
- Сравнивает с **ImpactGoal**
- Публикуется для инвесторов и регуляторов
- Записывается в **Blockchain** для прозрачности

### 6. User (Пользователь)

**Описание**: Пользователи системы с различными ролями.

**Основные атрибуты**:
- `username` - имя пользователя
- `email` - email
- `userType` - тип пользователя (ISSUER, INVESTOR, AUDITOR, ADMIN)
- `roles` - роли пользователя
- `status` - статус (ACTIVE, LOCKED, EXPIRED, INACTIVE)
- `failedLoginAttempts` - количество неудачных попыток входа
- `accountLockedUntil` - время блокировки аккаунта

**Жизненный цикл**:

```
Создание → ACTIVE → LOCKED (при brute force) → ACTIVE (после разблокировки)
         → EXPIRED (истечение срока) → INACTIVE (деактивация)
```

**Взаимодействие**:
- Аутентифицируется через **API Gateway**
- Создает и управляет **Bond** (ISSUER)
- Верифицирует **Document** (AUDITOR)
- Просматривает **ImpactMetric** и **ImpactReport** (INVESTOR)

## Взаимодействие сущностей

### Сценарий 1: Создание и верификация облигации

```
1. ISSUER создает Bond (DRAFT)
   ↓
2. ISSUER загружает Document (ESG_REPORT, FINANCIAL_STATEMENT, PROJECT_PROPOSAL)
   ↓
3. Document проходит обработку (UPLOADED → PROCESSING → EXTRACTION_COMPLETED)
   ↓
4. AUDITOR верифицирует Document (VERIFICATION_PENDING → VERIFIED)
   ↓
5. Bond переходит в PENDING_VERIFICATION → VERIFIED
   ↓
6. Blockchain Integration токенизирует Bond (VERIFIED → TOKENIZED)
   ↓
7. Bond становится ACTIVE (доступна для инвестирования)
```

### Сценарий 2: Мониторинг воздействия

```
1. IoT устройства или ручной ввод создают ImpactMetric
   ↓
2. ImpactMetric валидируется и оценивается качество данных
   ↓
3. ImpactMetric сохраняется в MongoDB и InfluxDB
   ↓
4. ImpactMetric записывается в Blockchain для прозрачности
   ↓
5. Система сравнивает ImpactMetric с ImpactGoal
   ↓
6. Обновляется progressPercentage в ImpactGoal
   ↓
7. Генерируется ImpactReport (периодически)
   ↓
8. ImpactReport публикуется для инвесторов
```

### Сценарий 3: Полный жизненный цикл облигации

```
Создание Bond (DRAFT)
    ↓
Загрузка документов → Верификация документов
    ↓
Верификация Bond (VERIFIED)
    ↓
Токенизация на блокчейне (TOKENIZED)
    ↓
Активация (ACTIVE)
    ↓
Мониторинг метрик → Сравнение с целями → Генерация отчетов
    ↓
Погашение (MATURED) или Дефолт (DEFAULTED)
```

## Быстрый старт

### Предварительные требования

- Java 17 или выше
- Maven 3.8+
- Docker и Docker Compose
- PostgreSQL 15+ (или использование Docker)
- MongoDB 7+ (или использование Docker)
- Redis 7+ (или использование Docker)

### Запуск через Docker Compose

1. **Клонируйте репозиторий:**

```bash
git clone https://github.com/speculum-factorem/greenBondPlatform.git
cd green-bond-platform
```

2. **Соберите проект:**

```bash
mvn clean install -DskipTests
```

3. **Запустите все сервисы через Docker Compose:**

```bash
docker-compose up -d
```

4. **Проверьте статус сервисов:**

```bash
docker-compose ps
```

5. **Проверьте health checks:**

```bash
curl http://localhost:8080/actuator/health
```

### Запуск вручную (для разработки)

1. **Запустите базы данных:**

```bash
# PostgreSQL
docker run -d --name postgres -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres:15-alpine

# MongoDB
docker run -d --name mongodb -p 27017:27017 mongo:7.0

# Redis
docker run -d --name redis -p 6379:6379 redis:7-alpine
```

2. **Запустите сервисы по порядку:**

```bash
# 1. Platform Core (сборка)
cd platform-core && mvn clean install

# 2. Blockchain Integration
cd ../blockchain-integration && mvn spring-boot:run

# 3. Bond Issuance Service
cd ../bond-issuance-service && mvn spring-boot:run

# 4. Document Verification Service
cd ../document-verification-service && mvn spring-boot:run

# 5. Impact Monitoring Service
cd ../impact-monitoring-service && mvn spring-boot:run

# 6. API Gateway
cd ../api-gateway && mvn spring-boot:run
```

## API Документация

### Swagger UI

После запуска сервисов, документация доступна по следующим адресам:

- **API Gateway:** http://localhost:8080/swagger-ui.html
- **Bond Issuance Service:** http://localhost:8081/swagger-ui.html
- **Document Verification Service:** http://localhost:8083/swagger-ui.html
- **Impact Monitoring Service:** http://localhost:8084/swagger-ui.html
- **Blockchain Integration:** http://localhost:8082/swagger-ui.html

### Основные эндпоинты

#### Аутентификация (API Gateway)

```bash
# Вход в систему
POST /api/v1/auth/login
Content-Type: application/json

{
  "username": "issuer1",
  "password": "password123"
}

# Валидация токена
POST /api/v1/auth/validate
Authorization: Bearer <token>
```

#### Управление облигациями

```bash
# Создание облигации
POST /api/v1/bonds
Authorization: Bearer <token>
Content-Type: application/json

{
  "projectName": "Solar Energy Project",
  "issuanceAmount": 1000000,
  "interestRate": 3.5,
  "maturityDate": "2025-12-31"
}

# Получение списка облигаций
GET /api/v1/bonds?page=0&size=20
Authorization: Bearer <token>
```

#### Загрузка документов

```bash
# Загрузка документа
POST /api/v1/documents/upload
Authorization: Bearer <token>
Content-Type: multipart/form-data

{
  "bondId": "bond-123",
  "documentName": "ESG Report",
  "documentType": "ESG_REPORT",
  "file": <file>
}
```

#### Мониторинг метрик

```bash
# Создание метрики
POST /api/v1/impact/metrics
Authorization: Bearer <token>
Content-Type: application/json

{
  "bondId": "bond-123",
  "metricType": "CARBON_EMISSIONS",
  "value": 150.5,
  "unit": "tons",
  "recordedAt": "2024-01-15T10:00:00"
}
```

## Безопасность

### Аутентификация и авторизация

- **JWT токены** с настраиваемым секретом
- **BCrypt** хеширование паролей (strength 12)
- **Защита от brute force** (5 попыток → блокировка на 30 мин)
- **Ролевая модель доступа:**
  - `ISSUER` - эмитенты облигаций
  - `INVESTOR` - инвесторы
  - `AUDITOR` - аудиторы
  - `ADMIN` - администраторы
  - `USER` - обычные пользователи

### Тестовые пользователи

При первом запуске создаются тестовые пользователи:

| Username  | Password    | Role     |
| --------- | ----------- | -------- |
| issuer1   | password123 | ISSUER   |
| investor1 | password123 | INVESTOR |
| auditor1  | password123 | AUDITOR  |
| admin     | admin123    | ADMIN    |

## 📊 Логирование

### MDC (Mapped Diagnostic Context)

Все сервисы используют MDC для трейсинга запросов:

- `requestId` - уникальный ID запроса
- `userId` - ID пользователя
- `username` - имя пользователя
- `clientIp` - IP адрес клиента
- `userAgent` - User-Agent браузера
- `method` - HTTP метод
- `path` - путь запроса

### Формат логов

```
%5p [%X{requestId}, %X{username}, %X{clientIp}] %m%n
```

Пример:

```
INFO [abc123, issuer1, 192.168.1.1] Creating new bond for project: Solar Energy
```

##  Тестирование

### Запуск тестов

```bash
# Все тесты
mvn test

# Тесты конкретного модуля
cd platform-core && mvn test

# С пропуском тестов
mvn clean install -DskipTests
```

### Покрытие тестами

Проект включает:

- Unit тесты для всех сервисов
- Integration тесты для основных компонентов
- Тесты для утилит и валидации
- Тесты для контроллеров

## 🐳 Docker

### Docker Compose

Проект включает `docker-compose.yml` для запуска всех сервисов:

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f api-gateway

# Пересборка
docker-compose up -d --build
```

### Dockerfile

Каждый сервис имеет свой Dockerfile:

- Multi-stage сборка
- Минимальный образ (Alpine Linux)
- Непривилегированный пользователь
- Health checks

## Конфигурация

### Переменные окружения

Основные переменные окружения:

```bash
# JWT
JWT_SECRET=your-secret-key
JWT_EXPIRATION=3600

# Базы данных
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres

MONGODB_HOST=localhost
MONGODB_PORT=27017

REDIS_HOST=localhost
REDIS_PORT=6379

# Сервисы
BOND_ISSUANCE_SERVICE_URL=http://localhost:8081
IMPACT_MONITORING_SERVICE_URL=http://localhost:8084
DOCUMENT_VERIFICATION_SERVICE_URL=http://localhost:8083
```

### Профили Spring

- `default` - локальная разработка
- `docker` - для Docker контейнеров
- `prod` - production

## Технологический стек

### Backend

- **Spring Boot 3.2** - основной фреймворк
- **Spring Cloud Gateway** - API Gateway
- **Spring Security** - безопасность
- **Spring Data** - работа с БД (JPA, MongoDB, R2DBC)
- **Web3j** - интеграция с Ethereum
- **gRPC** - межсервисная коммуникация
- **MapStruct** - маппинг объектов
- **Apache Tika** - обработка документов

### Базы данных

- **PostgreSQL 15** - реляционная БД (облигации)
- **MongoDB 7** - документная БД (документы, метрики)
- **InfluxDB 2.7** - временные ряды (метрики)
- **Redis 7** - кэширование и rate limiting
- **H2** - in-memory БД (пользователи для pet проекта)

### Интеграции

- **Blockchain (Ethereum)** - токенизация и неизменяемость
- **IoT платформы** - сбор метрик (структура готова)
- **Apache Tika** - извлечение данных из документов

### Инструменты разработки

- **Maven** - управление зависимостями
- **Docker** - контейнеризация
- **JUnit 5** - тестирование
- **Lombok** - уменьшение boilerplate кода
- **Swagger/OpenAPI** - документация API

## Архитектурные паттерны

### Используемые паттерны:

1. **Микросервисная архитектура**

   - Разделение на независимые сервисы
   - API Gateway для единой точки входа
   - Service Discovery (готовность к Eureka)

2. **Domain-Driven Design (DDD)**

   - Разделение на домены
   - Репозитории для доступа к данным
   - Сервисы для бизнес-логики

3. **Clean Architecture**

   - Разделение слоев (Controller, Service, Repository)
   - Независимость от фреймворков
   - Тестируемость

4. **SOLID принципы**
   - Single Responsibility
   - Open/Closed
   - Liskov Substitution
   - Interface Segregation
   - Dependency Inversion

## Мониторинг и метрики

### Actuator Endpoints

Все сервисы предоставляют Actuator endpoints:

- `/actuator/health` - health check
- `/actuator/info` - информация о сервисе
- `/actuator/metrics` - метрики приложения

### Логирование

- Структурированное логирование с MDC
- Трейсинг запросов через все сервисы
- Централизованное логирование (готовность)

## CI/CD

Проект использует GitHub Actions для непрерывной интеграции и развертывания.

### GitHub Actions Workflows

#### 1. CI (Continuous Integration) - `.github/workflows/ci.yml`

Автоматически запускается при push и pull request в ветки `main` и `develop`:

- **Сборка и тестирование**: Компиляция всех модулей и запуск тестов
- **Проверка качества кода**: Анализ кода на наличие TODO/FIXME
- **Сборка Docker образов**: Создание Docker образов для всех сервисов
- **Сканирование безопасности**: OWASP Dependency Check для проверки уязвимостей

**Сервисы для тестирования**:
- PostgreSQL 15 (для Bond Issuance Service)
- MongoDB 7 (для Document Verification и Impact Monitoring)
- Redis 7 (для кэширования)

#### 2. CD (Continuous Deployment) - `.github/workflows/cd.yml`

Автоматически запускается при:
- Push в ветку `main`
- Создании тега версии (v*)
- Ручном запуске через workflow_dispatch

**Этапы**:
1. Сборка и публикация Docker образов в GitHub Container Registry
2. Деплой в staging (автоматически при push в main)
3. Деплой в production (при создании тега версии)
4. Создание GitHub Release

#### 3. Docker Build - `.github/workflows/docker-build.yml`

Специализированный workflow для сборки Docker образов:
- Запускается при изменении Dockerfile или исходного кода
- Кэширование слоев для ускорения сборки
- Поддержка multi-platform (amd64, arm64)

#### 4. Lint - `.github/workflows/lint.yml`

Проверка качества кода:
- Проверка форматирования Java кода
- Линтинг YAML файлов
- Линтинг Dockerfile

### Использование

#### Локальная проверка перед push

```bash
# Запуск тестов
mvn clean test

# Проверка форматирования
mvn spotless:check

# Сборка Docker образов
docker-compose build
```

#### Ручной запуск деплоя

1. Перейдите в раздел "Actions" в GitHub
2. Выберите workflow "CD"
3. Нажмите "Run workflow"
4. Выберите окружение (staging/production)
5. Запустите workflow

#### Создание релиза

```bash
# Создайте тег версии
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0
```

Это автоматически запустит CD pipeline и создаст GitHub Release.

### GitHub Container Registry

Docker образы публикуются в GitHub Container Registry:

```
ghcr.io/<owner>/api-gateway:latest
ghcr.io/<owner>/bond-issuance-service:latest
ghcr.io/<owner>/document-verification-service:latest
ghcr.io/<owner>/impact-monitoring-service:latest
ghcr.io/<owner>/blockchain-integration:latest
```

### Переменные окружения для CI/CD

Следующие переменные можно настроить в GitHub Secrets:

- `DOCKER_REGISTRY_USERNAME` - имя пользователя для Docker registry
- `DOCKER_REGISTRY_PASSWORD` - пароль для Docker registry
- `KUBERNETES_CONFIG` - конфигурация Kubernetes (для деплоя)
- `DEPLOYMENT_ENV` - окружение деплоя

### Статус сборки

Бейджи статуса сборки можно добавить в README:

```markdown
![CI](https://github.com/speculum-factorem/greenBondPlatform/workflows/CI/badge.svg)
![CD](https://github.com/speculum-factorem/greenBondPlatform/workflows/CD/badge.svg)
```

## Best Practices

### Код

- Использование MDC для логирования
- Валидация входных данных
- Обработка ошибок
- Транзакционность операций
- Асинхронная обработка

### Безопасность

-  BCrypt для паролей
-  JWT токены
-  Защита от brute force
-  Валидация входных данных
-  CORS настройки

### Производительность

-  Кэширование (Redis)
-  Connection pooling
-  Асинхронная обработка
-  Пагинация результатов

## Известные проблемы и ограничения

1. **InfluxDB клиент** - требуется обновление версии или репозитория
2. **Frontend** - в разработке
3. **Eureka Service Discovery** - настроено, но не используется в docker-compose
4. **IoT интеграция** - структура готова, но требуется реальная интеграция

## Роли пользователей

### ISSUER (Эмитент)

- Создание облигаций
- Загрузка документов
- Просмотр своих облигаций

### INVESTOR (Инвестор)

- Просмотр доступных облигаций
- Инвестирование
- Мониторинг метрик

### AUDITOR (Аудитор)

- Верификация документов
- Проверка соответствия стандартам
- Генерация отчетов

### ADMIN (Администратор)

- Управление пользователями
- Настройка системы
- Мониторинг всех операций